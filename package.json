{
  "name": "vote-backend-vercel",
  "version": "1.0.0",
  "scripts": {
    "dev": "node server.js"  // Optional for local testing if you want
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "pg": "^8.11.3"  // For PostgreSQL
  },
  "engines": {
    "node": ">=14"
  }
}